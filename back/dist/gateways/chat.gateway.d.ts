import { JwtService } from "@nestjs/jwt";
import { OnGatewayConnection, OnGatewayDisconnect } from "@nestjs/websockets";
import { Socket } from "socket.io";
import { chatRoomService } from "src/chatRoom/chatRoom.service";
import { roomMessageService } from "src/chatRoom/roomMessage.service";
import { roomBannedUserService } from "src/chatRoom/roomsBannedUser.service";
import { User } from "src/entities/user.entity";
import { GamesService } from "src/games/game.service";
import { liveGameService } from "src/liveGame/liveGame.service";
import { messageService } from "src/messages/message.service";
import { notificationService } from "src/notification/notification.service";
import { UserService } from "src/user/user.service";
import { Repository } from "typeorm";
import gamePlayService from "./gamePlay.service";
export declare class moveData {
    player1: number;
    player2: number;
    movement: string;
}
export declare class chatGateway implements OnGatewayConnection, OnGatewayDisconnect {
    private messageServ;
    private userServ;
    private usersRepository;
    private liveGameServ;
    private gamePlaysServ;
    private roomMessageServ;
    private chatRoomServ;
    private readonly jwtService;
    private gameServ;
    private roomBannedUserServ;
    private notifServ;
    constructor(messageServ: messageService, userServ: UserService, usersRepository: Repository<User>, liveGameServ: liveGameService, gamePlaysServ: gamePlayService, roomMessageServ: roomMessageService, chatRoomServ: chatRoomService, jwtService: JwtService, gameServ: GamesService, roomBannedUserServ: roomBannedUserService, notifServ: notificationService);
    server: Socket;
    handleDisconnect(client: Socket): Promise<void>;
    handleConnection(client: Socket, ...args: any): Promise<void>;
    handleMessage(client: Socket, text: any): Promise<void>;
    matchmaking(client: Socket, body: any): Promise<void>;
    setInterval(client: Socket, test: any): Promise<void>;
    addWatcher(client: Socket, body: any): Promise<void>;
    playing(client: Socket, body: moveData): Promise<void>;
    leaving(client: Socket, test: any): Promise<void>;
    handleChannels(client: Socket, text: any): Promise<void>;
    creatChannel(client: Socket, data: any): Promise<void>;
    handleRoomMessage(client: Socket, data: any): Promise<void>;
    addUserToChannel(client: Socket, data: any): Promise<void>;
    handleNotification(client: Socket, data: any): Promise<void>;
    muteUser(client: Socket, data: any): Promise<void>;
    banUser(client: Socket, data: any): Promise<void>;
    changeUserName(client: Socket, data: any): Promise<void>;
}
